# Data Backend: sqlitle (localy), or postgres (Docker)
DB_BACKEND=sqlite


# PostgreSQL (only if DB_BACKEND=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rakuten_colors
DB_USER=postgres
DB_PASS=secret